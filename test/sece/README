[ Build ]

Compile libencloud setting organization and mode configuration. E.g:

qmake -r CONFIG+="exor modesece"
make

[ Further Extensions ]

- setup version info (see encloud/src/common/info.cpp)
- can use libencloud::Logger for logging (see encloud/src/main.cpp)
